# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignore bundler config.
/.bundle

# Ignore the default SQLite database.
/db/*.sqlite3
#/db/*.sqlite3-* →これをコメントアウトして、下の行を追加
/db/*.sqlite3-journal

#DBを指定していない場合にデフォルトで生成されるdb.sqlite3
#SQLite3 には、journal mode と wal mode というのがある。SQLite3 のデフォルトは wal mode だが、Prisma は journal mode を意図的に採用している。
#WAL mode を有効にしないかという Issue) https://github.com/prisma/prisma/issues/3303
#.db-journal のファイルは .db への書き込みが失敗した時のロールバック用に使われるらしい。
#.db-journal ファイルは、それがロールバックの待機状態である「ホット」とそれ以外の状態がある。
#↑↑メンターに確認する

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore pidfiles, but keep the directory.
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# Ignore uploaded files in development.
/storage/*
!/storage/.keep

/public/assets
.byebug_history

# Ignore master key for decrypting credentials and more.
/config/master.key

/public/packs
/public/packs-test
/node_modules
/yarn-error.log
yarn-debug.log*
.yarn-integrity
